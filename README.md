
  # Community Web Platform UI

  This project is now structured as a Next.js app (App Router).

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  
